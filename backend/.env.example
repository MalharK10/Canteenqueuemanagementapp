PORT=3000
JWT_SECRET=replace-with-strong-random-secret

# Use one option for database connection:
# Option 1: DATABASE_URL
DATABASE_URL=database-1.ct48yskeyr97.ap-south-1.rds.amazonaws.com/canteen
# Option 2: Individual values (only used when DATABASE_URL is not set)
DB_HOST=database-1.ct48yskeyr97.ap-south-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=canteen
DB_USER=postgres
DB_PASSWORD=Parth1947

# Set true for AWS RDS in most cases
DB_SSL=false

AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=replace-me
AWS_SECRET_ACCESS_KEY=replace-me
AWS_S3_BUCKET=replace-me
